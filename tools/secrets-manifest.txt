# InstantML local-dev secrets manifest.
#
# Format: <env-var-name> <gcp-secret-id> [optional]
# - Lines starting with `#` and blank lines are ignored.
# - Whitespace-separated columns.
# - A third column "optional" marks a secret that may be missing without
#   failing `--check`. Defaults to required.
#
# This file is the single list `tools/load-secrets.sh` walks. To add a new
# secret to the local-dev environment:
#   1. `gcloud secrets create <id>` and add a version (see docs/ops/secrets.md).
#   2. Append a row here.
#   3. Re-run `source tools/load-secrets.sh`.
#
# Names mirror those in `tools/deploy-cloud-run.mjs::deploySecretSpecs()`.

# Clerk
CLERK_SECRET_KEY                                 instantml-clerk-secret-key
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY                instantml-clerk-publishable-key                   optional

# ClickHouse — User Data warehouse credentials
CLICKHOUSE_INSTANTML_USER_DATA_ENDPOINT          instantml-clickhouse-user-data-endpoint
CLICKHOUSE_INSTANTML_USER_DATA_USERNAME          instantml-clickhouse-user-data-username
CLICKHOUSE_INSTANTML_USER_DATA_PASSWORD          instantml-clickhouse-user-data-password

# ClickHouse Cloud control-plane API key
CLICKHOUSE_INSTANTML_GENERAL_KEY_ID              instantml-clickhouse-cloud-key-id
CLICKHOUSE_INSTANTML_GENERAL_KEY_SECRET          instantml-clickhouse-cloud-key-secret

# Bootstrap / admin (optional in local dev)
INSTANTML_BOOTSTRAP_TOKEN                        instantml-bootstrap-token                         optional

# Cloudflare R2 (artifact storage) — required only when running uploads locally
CLOUDFLARE_R2_API_KEY                            instantml-cloudflare-r2-api-key                   optional
CLOUDFLARE_API_TOKEN                             instantml-cloudflare-api-token                    optional
CLOUDFLARE_R2_ACCESS_KEY_ID                      instantml-cloudflare-r2-access-key-id             optional
CLOUDFLARE_R2_SECRET_ACCESS_KEY                  instantml-cloudflare-r2-secret-access-key         optional

# Stripe billing (optional in local dev)
STRIPE_SECRET_KEY                                instantml-stripe-secret-key                       optional
STRIPE_WEBHOOK_SECRET                            instantml-stripe-webhook-secret                   optional

# Transactional email (optional in local dev)
RESEND_API_KEY                                   instantml-resend-api-key                          optional
